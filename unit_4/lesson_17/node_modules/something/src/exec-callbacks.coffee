module.exports = execCallbacks = (keys, bindings, object) ->
  keys.reduce(([b, o], key) ->
    b = b?[key]
    o = o?[key]

    b?[CALLBACK_KEY]?.forEach (callback) ->
      setTimeout(->
        callback(o)
      , 0)

    [b, o]

  , [bindings, object])
