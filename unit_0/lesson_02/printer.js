"use strict";  
// Enables Strict Mode — this helps write safer, cleaner JavaScript code  
// by catching common errors (like using undeclared variables).

let x = "Universe";  
// Declares a variable named 'x' and assigns it the string value "Universe".

console.log(`Hello ${x}`);  
// Logs the message "Hello Universe" to the console.  
// The `${x}` syntax is called **template literal interpolation**,  
// which allows you to insert the value of the variable `x` directly into the string.
